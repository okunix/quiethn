apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quiethn.fullname" . }}-test-connection"
  labels:
    {{- include "quiethn.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['-qO-', '{{ include "quiethn.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
